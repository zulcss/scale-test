---
  NovaAvailabilityZones.list_availability_zones:
    -
      args:
        detailed: true
      runner:
        type: "constant"
        concurrency: {{concurrency}}
        times: {{times}}
      sla:
        failure_rate:
          max: 0
